---
title: الإنتاج
---

# **التحضير للإنتاج في إطار العمل كوكب Kawkab**

بعد الانتهاء من تطوير تطبيقك باستخدام **كوكب Kawkab**، ستحتاج إلى تشغيله في بيئة الإنتاج. هذا الدليل يشرح كيفية إعداد وتشغيل التطبيق باستخدام الأوامر المناسبة.

---

## **1. إنشاء نسخة الإنتاج (Build)**

لتحضير التطبيق ليعمل في بيئة الإنتاج، قم بإنشاء نسخة مضغوطة ومجهزة باستخدام الأمر التالي:

```bash
npm run build
```

هذا الأمر يقوم بـ:

- تجهيز الملفات النهائية التي يتم تشغيلها على الخادم.

ستُحفظ النسخة المولدة في المجلد `.dist`.

---

## **2. تشغيل التطبيق في بيئة الإنتاج**

بعد الانتهاء من البناء، يمكنك تشغيل التطبيق في وضع الإنتاج باستخدام الأمر التالي:

```bash
npm run start
```

### **ما يفعله هذا الأمر:**
- يقوم بتحميل النسخة النهائية من المشروع.
- يبدأ تشغيل خادم الإنتاج.
- يجعل التطبيق متاحًا على العنوان المحدد (عادةً `http://localhost:3000`).

---

## **3. نصائح هامة لبيئة الإنتاج**

### **إعدادات البيئة:**
تأكد من أن ملف البيئة `.env.production` يحتوي على الإعدادات المناسبة للإنتاج.

### **خادم الويب العكسي (Reverse Proxy):**
لتحسين الأداء والأمان، يمكنك وضع خادم الويب مثل **Nginx** أو **Apache** كوكيل عكسي أمام تطبيقك.

---

## **الختام**

بمجرد تشغيل التطبيق في وضع الإنتاج، سيكون جاهزًا للتعامل مع المستخدمين. إطار العمل **كوكب Kawkab** يقدم لك أدوات مرنة وفعّالة لضمان أداء عالٍ وتجربة مستخدم متميزة.

استمتع بالإنتاجية مع **كوكب Kawkab**! 🚀